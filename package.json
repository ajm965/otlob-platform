{
  "name": "otlob-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Otlob Platform monorepo",
  "workspaces": [
    "packages/core",
    "backend",
    "backend/functions"
  ],
  "scripts": {
    "build": "npm run build -w @otlob/core && npm run build -w @otlob/backend && npm run build -w @otlob/functions",
    "build:core": "npm run build -w @otlob/core",
    "build:domain": "npm run build -w @otlob/backend",
    "build:functions": "npm run build -w @otlob/functions"
  },
  "engines": {
    "node": ">=20"
  }
}

import type { EntityIdDto, MarketScopeDto } from '@otlob/core';

/** Output shape only; no behavior or conversion code is defined. */
export interface TechnicianResponse extends EntityIdDto, MarketScopeDto {}

# files/infrastructure

## Purpose

Adapters for persistence, external APIs, and cache for `files`.

## Responsibilities

- Repository implementations, datasources, models, mappers, cache

## Dependencies

- `files/application` interfaces and `files/domain` ports
- Firebase/PSP/SMS SDKs only when implementation is authorized later

## Ownership

Backend infrastructure owners for `files`

## Future Implementation Notes

- No Firebase configuration or SDK wiring in Phase 2.1

{
  "name": "@otlob/backend",
  "version": "0.1.0",
  "private": true,
  "description": "Otlob backend workspace — domain foundation (Sprint 2.3)",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:domain": "tsc -p tsconfig.domain.json",
    "test": "tsc -p tsconfig.json && node --test dist/modules/auth/tests/unit/*.test.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@otlob/core": "file:../packages/core"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}

# Security Rules (Reserved)

Firestore for:

- `firestore.rules`
- `storage.rules`
- `firestore.indexes.json`

Design baseline: `docs/SECURITY.md` and `docs/FIRESTORE_STRUCTURE.md`.

No rules implementation in Phase 1.

# UniqueId

## Status

Phase 2.2 placeholder — **not implemented**.

## Purpose

Opaque unique identifier value object for entity identity.

## Future API Surface (documentation only)

- `UniqueId`
- `fromString / generate (via IUuidGenerator later)`
- `value equality`

## Constraints

- No database-specific ID formats hard-coded to Firebase

## Implementation Gate

Do not add Dart/TypeScript classes, Firebase, HTTP, UI, or feature/business logic until a later phase explicitly authorizes implementation.

# exceptions/

## Purpose

Exception hierarchy for abort paths that cannot use `Result` (e.g., boundary adapters).

## Placeholders

AppException, ValidationException, UnauthorizedException, NotFoundException

## Ownership

Platform core library owners

## Phase Constraint

Placeholders only — no implemented classes.

# Domain event base (re-export/alias)

## Status

Phase 2.2 placeholder — **not implemented**.

## Purpose

Events folder for event contracts and helpers; base type documented under base/DomainEvent.

## Future API Surface (documentation only)

- `DomainEvent (see base/domain_event.md)`
- `EventMetadata (future generic)`

## Constraints

- No feature events

## Implementation Gate

Do not add Dart/TypeScript classes, Firebase, HTTP, UI, or feature/business logic until a later phase explicitly authorizes implementation.

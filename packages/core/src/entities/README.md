# entities/

## Purpose

Reserved for rare **generic** entity helpers shared across modules (if any).

## Responsibilities

- Do not place feature entities here (`User`, `Booking`, etc. belong in modules)
- Prefer `base/BaseEntity` and module-local entities

## Ownership

Platform core library owners

## Phase Constraint

Empty reserved folder in Phase 2.2 — no feature entities.
